{% extends 'base.html.twig' %}

{% block title %}{{ 'games.index.title'|trans }} — Abomey{% endblock %}

{% block body %}
    <section class="games-list-hero">
        <h1>{{ 'games.index.title'|trans }}</h1>
        <a href="{{ path('app_game_new') }}" role="button">{{ 'games.index.create'|trans }}</a>
    </section>

    {% if games is empty %}
        <article class="empty-state">
            <p>{{ 'games.index.empty.message'|trans }}</p>
        </article>
    {% else %}
        <ul class="game-cards">
            {% for game in games %}
                <li><twig:game_card :game="game" /></li>
            {% endfor %}
        </ul>
    {% endif %}
{% endblock %}
